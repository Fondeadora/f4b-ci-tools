
sudo wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash

# export NVM_DIR="$HOME/.nvm"
# sudo [ -s "$NVM_DIR/nvm.sh" ] && sudo \. "$NVM_DIR/nvm.sh"
# sudo nvm install $NODE_VERSION && sudo nvm use $NODE_VERSION
